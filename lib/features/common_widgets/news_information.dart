import 'package:era_developers_test_flutter/features/UI/pages/news_screen/widgets/latest_news.dart';
import 'package:era_developers_test_flutter/repositories/news/repository.dart';
import 'package:flutter/material.dart';

class NewsInformation extends StatelessWidget {
  const NewsInformation({
    super.key,
    // required this.article,
  });

  // final Article article;

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        // Text(
        //   // article.description!,
        // ),
      ],
    );
  }
}
